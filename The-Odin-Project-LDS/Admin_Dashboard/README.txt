LEGEND:
* is for topic
- is for continution of idea or theory
> is for after thoughts, can be as many

* What I learned :

CSS grid to layout out a known structure
Flexbox for buttons, user icon and name
    -May use it for annoucement and trending divs
I practice basic svg manipulation
    -The sidebar history <em>svg<em> is a clock, it could display actual

Article redesign:

Each article could be a grid of 3 rows
Justify the content to the start
Justify the last to end

>Remember to make one article work then complete the rest

REVERTED TO AN OLD COMMIT AND SUFFERED DATA LOSS

Fix : There was a bug about the navbar layout being bigger than what is displayed
First thought were about svg being wrongly scaled, learned a new method to scale them
-height and width can be assign in html, the viewbox numbers matters too, try changing them
What actually worked was adjusting the size of the grid rows(the navbar rows)
-Also made the containers first row small

Add : padding to odin icon

Fix : Side Bar function too centered, moved to the left edge
> also scaling down size of sidebar, font size do still look big
>>fixed "Dashboard" p element font size, gemini gave an h1 - h6 list
    of clamp sizes

Fix : small account -> added gap instead of 
    space-around to keep items right centered
    Also added some large padding to the right


